# Maestro E2E Tests

## Debug

```bash
maestro test maestro/happy_path.yaml
maestro test maestro/rotation_article_persistence.yaml
```

## Release

Edit `appId` in the YAML files from `com.mauromarod.spaceflightnews.debug` to `com.mauromarod.spaceflightnews`.
